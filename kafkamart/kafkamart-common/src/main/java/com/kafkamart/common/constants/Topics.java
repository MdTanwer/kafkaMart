package com.kafkamart.common.constants;

/**
 * Topic names are filled in by later prompts. Services must reference these constants
 * (or mp.messaging channel env vars) — never hard-code topic strings.
 */
public final class Topics {
    private Topics() {}
}
